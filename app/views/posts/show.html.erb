<%= render partial: "header" %>

<div class ="container-show">
    <%# <div class="col-md-12 card margin-top-5per  margin-bottom-5per book-block"> %>
      <%= image_tag @post.image.url, class: 'post-image' %>     
        <h3 class = "text-center"><%= @post.title%></h3>
          <div class = "padding-bottom-15">
            <div class = "time-like">
                        <div id="likes_buttons_<%= @post.id %>">
              <%= render partial: 'likes/like', locals: { post: @post, likes: @likes} %>
              </div>
            <div id="likes_buttons_<%= @post.id %>">
</div>
          <div class = "created_at"> <%= @post.created_at.strftime('%Y/%m/%d')%>


            </div>
  

              </div>
              <div class= "text-center title"><%= @post.body%></div> 



              <div class = "edit-delete">
                  <%= link_to "Edit", edit_post_path(@post) , style: 'color: lightgrey; text-decoration :none; font-size: 30px; padding-right: 30px;'%>

                  <%= link_to "Delete",post_path(@post), method: :delete, data: {confirm: '削除しますか？'}, style: 'color: lightgrey; text-decoration :none; font-size: 30px;'%>

              </div>

          </div>
        </div>

    <%# </div>   %>
  </div>

  <footer class="footer appear up">
          <div class="logo item">
            <span class="logo__stay">RE</span>
            <span class="logo__world">ISE</span>
          </div>
          <nav class="footer__nav">
            <ul class="footer__ul">
              <li class="footer__li item"><a href="#">Service</a></li>
              <li class="footer__li item"><a href="#">Contact</a></li>
              <li class="footer__li item"><a href="#">Company</a></li>
              <li class="footer__li item"><a href="#">Sitemap</a></li>
            </ul>
            <div class="footer__copyright item">
              &copy; REISE
            </div>
          </nav>
        </footer>